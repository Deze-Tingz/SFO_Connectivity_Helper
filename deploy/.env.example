# Shared secret for token signing between signaling and relay
SIGNALING_SECRET=your-secret-key-here-min-32-chars

# Session TTL in minutes (default: 15)
SESSION_TTL_MINUTES=15

# Max session duration in hours (default: 4)
MAX_SESSION_HOURS=4
